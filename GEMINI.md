# Gemini Instructions

Use `AGENTS.md` as the primary repository instruction file.

Gemini-specific notes:

- Work from the repository root: the directory containing `package.json`, `vite.config.ts`, `src/`, and `public/`.
- Inspect existing implementation patterns before changing code.
- Keep reusable template documentation neutral and portable across projects.
- Use the scripts defined in `package.json` for development, linting, builds, and code generation.
