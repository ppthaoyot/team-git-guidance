# Claude Instructions

Use `AGENTS.md` as the primary repository instruction file.

Claude-specific notes:

- Treat the repository root as the directory containing `package.json`, `vite.config.ts`, `src/`, and `public/`.
- Prefer editing existing files and patterns over creating new abstractions.
- Keep template-level guidance generic. Do not add project-specific product names, folder names, brands, domains, or business terms unless the user explicitly asks.
- Before finalizing code changes, run the most relevant validation command from `package.json` when practical.
